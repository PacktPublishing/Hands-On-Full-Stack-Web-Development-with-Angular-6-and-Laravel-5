import { BikeSearchPipe } from './bike-search.pipe';

describe('BikeSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BikeSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
